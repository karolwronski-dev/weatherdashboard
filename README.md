Dashboard using openmeto-api for my personal use + learning python, using vs code, windows
1. Install this
python -m pip install openmeteo-requests
python -m pip install requests-cache retry-requests numpy pandas
and run python .\main.py in terminal, voila!
2.
